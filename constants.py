from imports import *


class Constants:
    CAPTION = 'BaseApp'
    DIMENSIONS = WIDTH, HEIGHT = np.array((1500, 927))
    CENTRE = H_WIDTH, H_HEIGHT = DIMENSIONS / 2
    FRAMERATE = 80

    POINT_SIZE = 2
